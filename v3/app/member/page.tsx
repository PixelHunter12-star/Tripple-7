import {db} from "@/lib/db"; import {cookies} from "next/headers"; import {hashToken} from "@/lib/passcode"; import Link from "next/link";
export default async function Member(){const t=(await cookies()).get("tripple7_session")?.value;if(!t)return <main className="center"><section className="cardform"><h1>Login required</h1><Link className="btn primary" href="/login">Login</Link></section></main>;const s=await db.passcodeSession.findUnique({where:{tokenHash:hashToken(t)},include:{user:true}});if(!s||s.expiresAt<=new Date())return <main className="center"><section className="cardform"><h1>Session expired</h1><Link className="btn primary" href="/login">Login</Link></section></main>;const sub=await db.subscription.findFirst({where:{userId:s.userId,status:"ACTIVE",expiresAt:{gt:new Date()}},include:{plan:true},orderBy:{expiresAt:"desc"}});return <main className="shell"><nav className="nav"><b>777</b><span>Member Area</span><Link href="/trailers">Trailers</Link></nav><section className="dashboard"><p className="eyebrow">WELCOME</p><h1>{s.user.displayName}</h1><div className="grid"><div className="panel"><small>PLAN</small><b>{sub?.plan.name}</b></div><div className="panel"><small>EXPIRES</small><b>{sub?.expiresAt.toLocaleString()}</b></div></div></section></main>}